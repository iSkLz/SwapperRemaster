nothing = {
    type: CONST.module.nothing,
    data: {}
};

key = {
    type: CONST.module.key,
    data: {
        material: CONST.material.silver
    }
};

swap = {
    type: CONST.module.swap,
    data: {
        color: CONST.color.white
    }
};

swapitem = {
    type: CONST.module.nothing,
    data: {
        delta: 1
    }
};

rkey = {
    type: CONST.module.nothing,
    data: {
        color: CONST.rcolor.brown,
        shape: CONST.rshape.delta
    }
};

nothing = {
    type: CONST.module.nothing,
    data: {}
};

